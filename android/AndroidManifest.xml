<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".button.ButtonListener"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
        </activity>
        <!-- .textview -->
        <activity
            android:name=".textview.TextViewDemo"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".textview.TextViewForImg"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".textview.TextRunLamp"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".textview.TextViewOnClick"
            android:label="@string/app_name" >
        </activity>
        <!-- .editext -->
        <activity
            android:name=".editext.Editext"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>